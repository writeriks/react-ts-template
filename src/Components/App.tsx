import React from "react";

function App() {
  const classStyle = {
    display: "flex",
    justifyContent: "center",
    width: "100vw",
    height: "100vh",
    fontSize: 50,
  };

  return (
    <div style={classStyle}>
      <span>Project Template</span>
    </div>
  );
}

export default App;
